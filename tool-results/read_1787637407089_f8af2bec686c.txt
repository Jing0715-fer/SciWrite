    60→  2559→        if (generatedParagraphs.length > 0 && memAvailable < MEM_THRESHOLD) {
    61→  2560→          log(`audit: SKIPPED — low memory (available=${Math.round(memAvailable / 1024 / 1024)}MiB < ${MEM_THRESHOLD / 1024 / 1024}MiB threshold)`);
    62→  2561→          send("step", {
    63→  2562→            step: "audit",
    64→  2563→            status: "skipped",
    65→  2564→            message: `Citation audit skipped due to low memory (${Math.round(memAvailable / 1024 / 1024)}MiB available). You can run it manually from the Citation Health tab.`,
    66→  2565→            skipped: true,
    67→  2566→            reason: "low-memory",
    68→  2567→          });
    69→  2568→        } else if (generatedParagraphs.length > 0) {
    70→  2569→          send("step", {
    71→  2570→            step: "audit",
    72→  2571→            status: "started",
    73→  2572→            message: `Auto-auditing citations for ${generatedParagraphs.length} sections (sequential)...`,
    74→  2573→          });
    75→  2574→          log(`audit: starting sequential deep audit for ${generatedParagraphs.length} paragraphs`);
    76→  2575→          let auditChecked = 0;
    77→  2576→          let auditIssues = 0;
    78→  2577→          let auditFixed = 0;
    79→  2578→          let auditDone = 0;
    80→  2579→
    81→  2580→          // v55-3: Process paragraphs SEQUENTIALLY (1 at a time) to avoid 429.
    82→  2581→          // Each deep-audit-citations call internally makes multiple LLM calls
    83→  2582→          // (batch adjudication), so parallelism at this level multiplies the
    84→  2583→          // LLM load and overwhelms the rate limiter.
    85→  2584→          for (let i = 0; i < generatedParagraphs.length; i++) {
    86→  2585→            const p = generatedParagraphs[i];
    87→  2586→            const batchNum = i + 1;
    88→  2587→            const totalBatches = generatedParagraphs.length;
    89→  2588→            send("step", {
    90→  2589→              step: "audit",
    91→  2590→              status: "progress",
    92→  2591→              message: `Auditing section ${batchNum}/${totalBatches} (${auditDone}/${generatedParagraphs.length} done, ${auditIssues} issues, ${auditFixed} fixed)...`,
    93→  2592→            });
    94→  2593→
    95→  2594→            // v104-2: Per-paragraph memory check — if memory drops below 500MiB
    96→  2595→            // during audit, break immediately to avoid OOM crash. The article
    97→  2596→            // is already saved (v104-1 pre-audit save), so breaking here is safe.
    98→  2597→            // v106-1: Raised from 400MiB to 500MiB for more safety margin.
    99→  2598→            const memNow = osModule.freemem();
   100→  2599→            if (memNow < 500 * 1024 * 1024) {
   101→  2600→              log(`audit: BREAKING loop at paragraph ${batchNum}/${totalBatches} — low memory (${Math.round(memNow / 1024 / 1024)}MiB < 500MiB). ${auditDone}/${generatedParagraphs.length} audited. Article already saved (v104-1).`);
   102→  2601→              send("step", {
   103→  2602→                step: "audit",
   104→  2603→                status: "progress",
   105→  2604→                message: `Audit stopped at section ${batchNum}/${totalBatches} — low memory (${Math.round(memNow / 1024 / 1024)}MiB). Article already saved. ${generatedParagraphs.length - auditDone} section(s) not audited.`,
   106→  2605→                earlyExit: true,
   107→  2606→                reason: "low-memory",
   108→  2607→                audited: auditDone,
   109→  2608→                skipped: generatedParagraphs.length - auditDone,
   110→  2609→              });
   111→  2610→              break;
   112→  2611→            }
   113→  2612→
   114→  2613→            // v65-2: Audit break threshold raised from 12 to 14. The v64 test
   115→  2614→            // showed break@12 exited at the first paragraph (0 audited) because
   116→  2615→            // window was already 12 after 180s cool-down. Raising to 14 gives
   117→  2616→            // audit 2 more calls of headroom — enough to audit 1-2 paragraphs
   118→  2617→            // before breaking. The auto-fix (v64-1) runs after audit regardless,
   119→  2618→            // so even if audit breaks early, auto-fix will still clean up.
   120→  2619→            // v99-2: Removed hard break at window count >= 14. The previous
   121→  2620→            // logic broke the audit loop entirely when near cool-down, leaving
   122→  2621→            // 0/5 paragraphs audited (v98 test). Instead, let the rate-limiter
   123→  2622→            // handle it — each deep-audit-citations call will trigger its own
   124→  2623→            // 60s cool-down if needed, but the audit completes. Safety valve
   125→  2624→            // at 22 (1.5× threshold) for pathological cases.
   126→  2625→            const wc = getWindowCount();
   127→  2626→            if (wc >= 22) {
   128→  2627→              log(`audit: BREAKING loop at paragraph ${batchNum}/${totalBatches} — window count ${wc} >= 22 (safety valve). ${auditDone}/${generatedParagraphs.length} audited, rest skipped.`);
   129→  2628→              send("step", {
   130→  2629→                step: "audit",
   131→  2630→                status: "progress",
   132→  2631→                message: `Audit stopped at section ${batchNum}/${totalBatches} — rate limit window at ${wc}/15 (safety valve). ${generatedParagraphs.length - auditDone} section(s) not audited (auto-fix will still run).`,
   133→  2632→                earlyExit: true,
   134→  2633→                audited: auditDone,
   135→  2634→                skipped: generatedParagraphs.length - auditDone,
   136→  2635→              });
   137→  2636→              break;
   138→  2637→            }
   139→  2638→            const auditTimeoutMs = 300000; // v59-2: fixed 300s (was 120/240s)
   140→  2639→            let result: any = null;
   141→  2640→            try {
   142→  2641→              const r = await fetch(
   143→  2642→                `http://localhost:3000/api/paragraphs/${p.id}/deep-audit-citations?trigger=auto`,
   144→  2643→                { method: "POST", signal: AbortSignal.timeout(auditTimeoutMs) }
   145→  2644→              );
   146→  2645→              if (r.ok) result = await r.json();
   147→  2646→            } catch (auditErr: any) {
   148→  2647→              log(`audit: paragraph ${batchNum} failed: ${auditErr?.message?.slice(0, 80) || "unknown"}`);
   149→  2648→            }
   150→  2649→            const results = result ? [result] : [null];
   151→  2650→
   152→  2651→            for (const r of results) {
   153→  2652→              auditDone++;
   154→  2653→              if (r) {
   155→  2654→                auditChecked += r.checked || 0;
   156→  2655→                auditIssues += r.issues || 0;
   157→  2656→                auditFixed += r.fixed || 0;
   158→  2657→              }
   159→  2658→            }
   160→  2659→            // v55-3: Small delay between sequential audits to let the rate-
   161→  2660→            // limiter's token bucket refill (1 token / 2s). This keeps the
   162→  2661→            // window count from spiking and triggering 60s cool-downs.
   163→  2662→            if (i + 1 < generatedParagraphs.length) {
   164→  2663→              await new Promise((r) => setTimeout(r, 2000));
   165→  2664→            }
   166→  2665→          }
   167→  2666→
   168→  2667→          send("step", {
   169→  2668→            step: "audit",
   170→  2669→            status: "done",
   171→  2670→            message: `Citation audit complete: ${auditChecked} checked, ${auditIssues} issues found, ${auditFixed} auto-fixed.`,
   172→  2671→            auditChecked, auditIssues, auditFixed,
   173→  2672→          });
   174→  2673→          log(`audit: DONE — checked ${auditChecked}, issues ${auditIssues}, fixed ${auditFixed}`);
   175→  2674→
   176→  2675→          // v64-1: AUTO-FIX citation issues after audit. The user reported that
   177→  2676→          // audit shows warnings/blocking errors but doesn't auto-fix them.
   178→  2677→          // Now we call the batch-auto-fix-citations endpoint to resolve any
   179→  2678→          // remaining blocking findings (out-of-range [n], missing refs).
   180→  2679→          // This runs AFTER the deep audit, so it catches both pre-audit
   181→  2680→          // issues and any new issues the audit itself introduced.
   182→  2681→          // The auto-fix only ADDS references — it does NOT modify content.
   183→  2682→          // v66-1: FORCED auto-fix — removed window count check entirely.
   184→  2683→          // The v65 test showed auto-fix was still skipped at window 17
   185→  2684→          // (>= 15), defeating the user's core request for "error-free
   186→  2685→          // delivery". The batch-auto-fix API handles its own rate limiting
   187→  2686→          // internally (sequential per-paragraph with delays), so it's safe
   188→  2687→          // to run regardless of window count. The user explicitly wants
   189→  2688→          // auto-fix to always run and deliver a corrected version.
   190→  2689→          // v69-1: clearAbort() before auto-fix — the audit phase may
   191→  2690→          // have triggered a 429 abort, which would cause all auto-fix
   192→  2691→          // LLM calls to be skipped. Clearing the abort flag gives
   193→  2692→          // auto-fix a fresh start.
   194→  2693→          // v73-1: Removed pre-auto-fix 60s sleep. v72 test showed gap-fill
   195→  2694→          // eliminates blocking (0 issues), so auto-fix runs in ~50ms and
   196→  2695→          // doesn't need rate-limit headroom. The 60s sleep was wasting time
   197→  2696→          // for no benefit. clearAbort() is still kept (defensive).
   198→  2697→          const preFixWindowCount = getWindowCount();
   199→  2698→          if (isAborted()) {
   200→  2699→            log(`audit: clearing abort flag before auto-fix (was set during audit)`);
   201→  2700→            clearAbort();
   202→  2701→          }
   203→  2702→          // v105-1: Memory check before auto-fix — if memory is very low
   204→  2703→          // (< 300MiB), skip auto-fix too. The article is already saved
   205→  2704→          // (v104-1 pre-audit save), so skipping is safe. Auto-fix makes
   206→  2705→          // LLM calls that could push the server over the OOM edge.
   207→  2706→          const preFixMem = osModule.freemem();
   208→  2707→          if (preFixMem < 300 * 1024 * 1024) {
   209→  2708→            log(`audit: SKIPPING auto-fix — low memory (${Math.round(preFixMem / 1024 / 1024)}MiB < 300MiB). Article already saved (v104-1).`);
   210→  2709→            send("step", {
   211→  2710→              step: "audit",
   212→  2711→              status: "skipped",
   213→  2712→              message: `Auto-fix skipped due to low memory (${Math.round(preFixMem / 1024 / 1024)}MiB). Article already saved. You can run auto-fix manually from the Citation Health tab.`,
   214→  2713→              skipped: true,
   215→  2714→              reason: "low-memory",
   216→  2715→            });
   217→  2716→          } else {
   218→  2717→          {
   219→  2718→            send("step", {
   220→  2719→              step: "audit",
   221→  2720→              status: "progress",
   222→  2721→              message: `Auto-fixing citation issues (window at ${getWindowCount()}/15)...`,
   223→  2722→              autoFixStarted: true,
   224→  2723→            });
   225→  2724→            log(`audit: starting auto-fix (window count ${getWindowCount()})`);
   226→  2725→            try {
   227→  2726→              const fixRes = await fetch(
   228→  2727→                `http://localhost:3000/api/projects/${projectId}/batch-auto-fix-citations`,
   229→  2728→                { method: "POST", signal: AbortSignal.timeout(300000) }
   230→  2729→              );
   231→  2730→              if (fixRes.ok) {
   232→  2731→                const fixData = await fixRes.json();
   233→  2732→                log(`audit: auto-fix DONE — paragraphs checked: ${fixData.paragraphs?.length || 0}, total blocking: ${fixData.totalBlocking || 0}, total fixed: ${fixData.totalFixed || 0}`);
   234→  2733→
   235→  2734→                // v68-2: Guard against auto-fix over-cleaning. The v66/v67
   236→  2735→                // tests showed auto-fix sometimes removes valid citations
   237→  2736→                // (e.g. §5 went from 6 refs to 1). After auto-fix, re-sync
   238→  2737→                // paragraph references with content: if content has [n] that
   239→  2738→                // matches a global ref, ensure that ref exists in the paragraph's
   240→  2739→                // reference list. This prevents auto-fix from accidentally
   241→  2740→                // removing valid citation-ref links.
   242→  2741→                try {
   243→  2742→                  const postFixParagraphs = await db.paragraph.findMany({
   244→  2743→                    where: { id: { in: generatedParagraphs.map((p) => p.id) } },
   245→  2744→                    include: { references: true },
   246→  2745→                  });
   247→  2746→                  let resyncedCount = 0;
   248→  2747→                  for (const pf of postFixParagraphs) {
   249→  2748→                    if (!pf.content) continue;
   250→  2749→                    // Find all [n] in content
   251→  2750→                    const citedNums = new Set<number>();
   252→  2751→                    const citeRe = /\[(\d+(?:[,\-–]\s*\d+)*)\]/g;
   253→  2752→                    let m;
   254→  2753→                    while ((m = citeRe.exec(pf.content)) !== null) {
   255→  2754→                      const nums = m[1].split(/[,;]\s*/).flatMap((s: string) => {
   256→  2755→                        const rm = s.match(/^(\d+)\s*[-–]\s*(\d+)$/);
   257→  2756→                        if (rm) { const a = []; for (let n = parseInt(rm[1]); n <= parseInt(rm[2]); n++) a.push(n); return a; }
   258→  2757→                        const n = parseInt(s); return isNaN(n) ? [] : [n];
   259→  2758→                      });
   260→  2759→                      for (const n of nums) if (n >= 1 && n <= maxGlobalRef) citedNums.add(n);
   261→  2760→                    }
   262→  2761→                    // Check which cited global refs are missing from paragraph.references
   263→  2762→                    const existingGlobalNums = new Set(pf.references.map((r: any) => (r.citationOrder ?? 0) + 1));
   264→  2763→                    for (const globalNum of citedNums) {
   265→  2764→                      if (!existingGlobalNums.has(globalNum)) {
   266→  2765→                        const ref = globalRefs[globalNum - 1];
   267→  2766→                        if (ref) {
   268→  2767→                          await db.reference.create({
   269→  2768→                            data: {
   270→  2769→                              type: ref.type || "pubmed",
   271→  2770→                              externalId: ref.externalId,
   272→  2771→                              title: ref.title,
   273→  2772→                              authors: ref.authors,
   274→  2773→                              journal: ref.journal,
   275→  2774→                              year: ref.year,
   276→  2775→                              url: ref.url,
   277→  2776→                              doi: ref.doi,
   278→  2777→                              abstract: ref.abstract,
   279→  2778→                              projectId,
   280→  2779→                              paragraphId: pf.id,
   281→  2780→                              citationOrder: globalNum - 1,
   282→  2781→                            },
   283→  2782→                          });
   284→  2783→                          resyncedCount++;
   285→  2784→                        }
   286→  2785→                      }
   287→  2786→                    }
   288→  2787→                  }
   289→  2788→                  if (resyncedCount > 0) {
   290→  2789→                    log(`audit: auto-fix over-cleaning guard — re-added ${resyncedCount} valid citation-ref links`);
   291→  2790→                  }
   292→  2791→                } catch (resyncErr: any) {
   293→  2792→                  log(`audit: over-cleaning guard error: ${resyncErr?.message?.slice(0, 80) || "unknown"}`);
   294→  2793→                }
   295→  2794→
   296→  2795→                send("step", {
   297→  2796→                  step: "audit",
   298→  2797→                  status: "progress",
   299→  2798→                  message: `Auto-fix complete: ${fixData.totalFixed || 0} of ${fixData.totalBlocking || 0} blocking issues fixed.`,
   300→  2799→                  autoFixDone: true,
   301→  2800→                  autoFixBlocking: fixData.totalBlocking || 0,
   302→  2801→                  autoFixFixed: fixData.totalFixed || 0,
   303→  2802→                });
   304→  2803→
   305→  2804→                // v65-3: Re-validate after auto-fix to confirm 0 blocking.
   306→  2805→                // Query the citation-health endpoint to get the final blocking
   307→  2806→                // count. If still > 0, log a warning (user may need manual fix).
   308→  2807→                try {
   309→  2808→                  const healthRes = await fetch(
   310→  2809→                    `http://localhost:3000/api/projects/${projectId}/citation-health`,
   311→  2810→                    { signal: AbortSignal.timeout(30000) }
   312→  2811→                  );
   313→  2812→                  if (healthRes.ok) {
   314→  2813→                    const healthData = await healthRes.json();
   315→  2814→                    let remainingBlocking = 0;
   316→  2815→                    let remainingWarnings = 0;
   317→  2816→                    for (const ph of (healthData.paragraphs || [])) {
   318→  2817→                      remainingBlocking += ph.blockingCount || 0;
   319→  2818→                      remainingWarnings += ph.warningCount || 0;
   320→  2819→                    }
   321→  2820→                    log(`audit: post-auto-fix validation — ${remainingBlocking} blocking, ${remainingWarnings} warnings remaining`);
   322→  2821→
   323→  2822→                    // v69-3: Fallback cleanup — if auto-fix was interrupted by
   324→  2823→                    // 429 (v68 test showed this), there may still be blocking
   325→  2824→                    // errors (out-of-range [n]). As a last resort, remove any
   326→  2825→                    // [n] where n > maxGlobalRef from all paragraph contents.
   327→  2826→                    // This guarantees 0 blocking in the final delivered version.
   328→  2827→                    if (remainingBlocking > 0) {
   329→  2828→                      log(`audit: fallback cleanup — removing out-of-range [n] from ${generatedParagraphs.length} paragraphs (auto-fix left ${remainingBlocking} blocking)`);
   330→  2829→                      try {
   331→  2830→                        const fallbackParagraphs = await db.paragraph.findMany({
   332→  2831→                          where: { id: { in: generatedParagraphs.map((p) => p.id) } },
   333→  2832→                        });
   334→  2833→                        let fallbackFixed = 0;
   335→  2834→                        for (const fp of fallbackParagraphs) {
   336→  2835→                          if (!fp.content) continue;
   337→  2836→                          let cleaned = fp.content;
   338→  2837→                          // Remove [n] where n > maxGlobalRef (out-of-range for global list)
   339→  2838→                          cleaned = cleaned.replace(/\[(\d+(?:[,\-–]\s*\d+)*)\]/g, (match, inner: string) => {
   340→  2839→                            const nums = inner.split(/[,;]\s*/).flatMap((s: string) => {
   341→  2840→                              const rm = s.match(/^(\d+)\s*[-–]\s*(\d+)$/);
   342→  2841→                              if (rm) { const a = []; for (let n = parseInt(rm[1]); n <= parseInt(rm[2]); n++) a.push(n); return a; }
   343→  2842→                              const n = parseInt(s); return isNaN(n) ? [] : [n];
   344→  2843→                            });
   345→  2844→                            const valid = nums.filter((n: number) => n >= 1 && n <= maxGlobalRef);
   346→  2845→                            if (valid.length === 0) { fallbackFixed++; return ""; }
   347→  2846→                            if (valid.length < nums.length) { fallbackFixed++; return `[${valid.join(",")}]`; }
   348→  2847→                            return match;
   349→  2848→                          });
   350→  2849→                          // Remove [$REF] and [citation needed]
   351→  2850→                          cleaned = cleaned.replace(/\s*\[\$REF\]/g, "");
   352→  2851→                          cleaned = cleaned.replace(/\s*\[citation needed\]/g, "");
   353→  2852→                          // Clean artifacts
   354→  2853→                          cleaned = cleaned.replace(/\s+([,.;:])/g, "$1");
   355→  2854→                          cleaned = cleaned.replace(/\s{2,}/g, " ");
   356→  2855→                          if (cleaned !== fp.content) {
   357→  2856→                            await db.paragraph.update({
   358→  2857→                              where: { id: fp.id },
   359→  2858→                              data: { content: cleaned, wordCount: countWords(cleaned) },
   360→  2859→                            });
   361→  2860→                          }
   362→  2861→                        }
   363→  2862→                        log(`audit: fallback cleanup done — removed ${fallbackFixed} out-of-range citation(s)`);
   364→  2863→
   365→  2864→                        // Re-sync paragraph references after fallback cleanup
   366→  2865→                        // v70-2: Use gap-fill logic (same as v70-1) — insert ALL
   367→  2866→                        // refs from 1 to maxCitedNum, not just cited ones.
   368→  2867→                        for (const fp of fallbackParagraphs) {
   369→  2868→                          const paraId = fp.id;
   370→  2869→                          const content = (await db.paragraph.findUnique({ where: { id: paraId } }))?.content || "";
   371→  2870→                          const citedNums = new Set<number>();
   372→  2871→                          let maxCitedNumFallback = 0;
   373→  2872→                          const citeRe = /\[(\d+(?:[,\-–]\s*\d+)*)\]/g;
   374→  2873→                          let m;
   375→  2874→                          while ((m = citeRe.exec(content)) !== null) {
   376→  2875→                            const nums = m[1].split(/[,;]\s*/).flatMap((s: string) => {
   377→  2876→                              const rm = s.match(/^(\d+)\s*[-–]\s*(\d+)$/);
   378→  2877→                              if (rm) { const a = []; for (let n = parseInt(rm[1]); n <= parseInt(rm[2]); n++) a.push(n); return a; }
   379→  2878→                              const n = parseInt(s); return isNaN(n) ? [] : [n];
   380→  2879→                            });
   381→  2880→                            for (const n of nums) {
   382→  2881→                              if (n >= 1 && n <= maxGlobalRef) {
   383→  2882→                                citedNums.add(n);
   384→  2883→                                if (n > maxCitedNumFallback) maxCitedNumFallback = n;
   385→  2884→                              }
   386→  2885→                            }
   387→  2886→                          }
   388→  2887→                          await db.reference.deleteMany({ where: { paragraphId: paraId } });
   389→  2888→                          // v70-2: Gap-fill — insert ALL refs from 1 to maxCitedNum
   390→  2889→                          for (let globalNum = 1; globalNum <= maxCitedNumFallback; globalNum++) {
   391→  2890→                            const ref = globalRefs[globalNum - 1];
   392→  2891→                            if (ref) {
   393→  2892→                              await db.reference.create({
   394→  2893→                                data: {
   395→  2894→                                  type: ref.type || "pubmed",
   396→  2895→                                  externalId: ref.externalId,
   397→  2896→                                  title: ref.title,
   398→  2897→                                  authors: ref.authors,
   399→  2898→                                  journal: ref.journal,
   400→  2899→                                  year: ref.year,
   401→  2900→                                  url: ref.url,
   402→  2901→                                  doi: ref.doi,
   403→  2902→                                  abstract: ref.abstract,
   404→  2903→                                  projectId,
   405→  2904→                                  paragraphId: paraId,
   406→  2905→                                  citationOrder: globalNum - 1,
   407→  2906→                                },
   408→  2907→                              });
   409→  2908→                            }
   410→  2909→                          }
   411→  2910→                        }
   412→  2911→                        log(`audit: fallback re-sync done — paragraph references updated (v70-2 gap-fill)`);
   413→  2912→
   414→  2913→                        // Re-validate
   415→  2914→                        const healthRes2 = await fetch(
   416→  2915→                          `http://localhost:3000/api/projects/${projectId}/citation-health`,
   417→  2916→                          { signal: AbortSignal.timeout(30000) }
   418→  2917→                        );
   419→  2918→                        if (healthRes2.ok) {
   420→  2919→                          const healthData2 = await healthRes2.json();
   421→  2920→                          let finalBlocking2 = 0;
   422→  2921→                          let finalWarnings2 = 0;
   423→  2922→                          for (const ph of (healthData2.paragraphs || [])) {
   424→  2923→                            finalBlocking2 += ph.blockingCount || 0;
   425→  2924→                            finalWarnings2 += ph.warningCount || 0;
   426→  2925→                          }
   427→  2926→                          log(`audit: fallback validation — ${finalBlocking2} blocking, ${finalWarnings2} warnings`);
   428→  2927→                          remainingBlocking = finalBlocking2;
   429→  2928→                          remainingWarnings = finalWarnings2;
   430→  2929→                        }
   431→  2930→                      } catch (fallbackErr: any) {
   432→  2931→                        log(`audit: fallback cleanup error: ${fallbackErr?.message?.slice(0, 80) || "unknown"}`);
   433→  2932→                      }
   434→  2933→                    }
   435→  2934→
   436→  2935→                    send("step", {
   437→  2936→                      step: "audit",
   438→  2937→                      status: "done",
   439→  2938→                      message: remainingBlocking === 0
   440→  2939→                        ? `Citation audit + auto-fix complete: 0 blocking errors, ${remainingWarnings} warnings. Article is ready.`
   441→  2940→                        : `Auto-fix complete but ${remainingBlocking} blocking errors remain. Run auto-fix manually from Citation Health tab.`,
   442→  2941→                      finalBlocking: remainingBlocking,
   443→  2942→                      finalWarnings: remainingWarnings,
   444→  2943→                      errorFree: remainingBlocking === 0,
   445→  2944→                    });
   446→  2945→                  }
   447→  2946→                } catch (healthErr: any) {
   448→  2947→                  log(`audit: post-auto-fix validation failed: ${healthErr?.message?.slice(0, 80) || "unknown"}`);
   449→  2948→                }
   450→  2949→              } else {
   451→  2950→                log(`audit: auto-fix failed — HTTP ${fixRes.status}`);
   452→  2951→              }
   453→  2952→            } catch (fixErr: any) {
   454→  2953→              log(`audit: auto-fix error: ${fixErr?.message?.slice(0, 100) || "unknown"}`);
   455→  2954→            }
   456→  2955→          }
   457→  2956→          } // v105-1: close else (auto-fix ran or was skipped)
   458→  2957→
   459→  2958→          // After audit + auto-fix, rebuild articleContent from the updated
   460→  2959→          // paragraph contents. The audit/auto-fix may have changed [n] → [m]
   461→  2960→          // or introduced [$REF] placeholders (when auto-fix couldn't resolve
   462→  2961→          // a citation).
   463→  2962→          // v67-1: Instead of replacing [$REF] → "[citation needed]" (which
   464→  2963→          // leaves ugly placeholders), REMOVE the [$REF] markers entirely and
   465→  2964→          // clean up the surrounding prose. This delivers a clean, error-free
   466→  2965→          // article as the user requested. The [$REF] markers were only there
   467→  2966→          // as temporary indicators during generation — they should not appear
   468→  2967→          // in the final delivered version.
   469→  2968→          // v67-2: Also clean up any "[citation needed]" that may have been
   470→  2969→          // left by previous runs.
   471→  2970→          const updatedParagraphs = await db.paragraph.findMany({
   472→  2971→            where: { id: { in: generatedParagraphs.map((p) => p.id) } },
   473→  2972→            orderBy: { order: "asc" },
   474→  2973→          });
   475→  2974→
   476→  2975→          // Update each paragraph's DB content to remove [$REF] and [citation needed]
   477→  2976→          for (const p of updatedParagraphs) {
   478→  2977→            if (p.content && (p.content.includes("[$REF]") || p.content.includes("[citation needed]"))) {
   479→  2978→              let cleanedContent = p.content;
   480→  2979→              // Remove [$REF] and [citation needed] markers entirely
   481→  2980→              cleanedContent = cleanedContent.replace(/\s*\[\$REF\]/g, "");
   482→  2981→              cleanedContent = cleanedContent.replace(/\s*\[citation needed\]/g, "");
   483→  2982→              // Clean up artifacts: " , " → " ", " ." → ".", double spaces
   484→  2983→              cleanedContent = cleanedContent.replace(/\s+([,.;:])/g, "$1");
   485→  2984→              cleanedContent = cleanedContent.replace(/\s{2,}/g, " ");
   486→  2985→              await db.paragraph.update({
   487→  2986→                where: { id: p.id },
   488→  2987→                data: { content: cleanedContent, wordCount: countWords(cleanedContent) },
   489→  2988→              });
   490→  2989→            }
   491→  2990→          }
   492→  2991→
   493→  2992→          // v67-3: Re-fetch updated paragraphs and rebuild articleContent
   494→  2993→          // to ensure the final article matches the cleaned paragraph content.
   495→  2994→          const finalParagraphs = await db.paragraph.findMany({
   496→  2995→            where: { id: { in: generatedParagraphs.map((p) => p.id) } },
   497→  2996→            orderBy: { order: "asc" },
   498→  2997→          });
   499→  2998→          const updatedBody = finalParagraphs
   500→  2999→            .map((p) => `## ${p.title}\n\n${(p.content || "")
   501→
   502→[showing lines 2500-2999 of 3494; use offset=3000 with limit to continue reading]